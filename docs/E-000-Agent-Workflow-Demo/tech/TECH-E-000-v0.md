# TECH Epic - v0（E-000）

> 建议文件路径：`/docs/E-000-Agent-Workflow-Demo/tech/TECH-E-000-v0.md`
>
> 目标：把示例 PRD/Story/Slice 翻译成可运行的静态原型与文档链路，无需后端。

---

## 0. 文档元信息

- EPIC_ID：`E-000`
- EPIC_DIR：`E-000-Agent-Workflow-Demo`
- 版本：`v0`
- 状态：`DRAFT`
- 作者：`@demo`
- 关联文档：
  - prd：`../prd/PRD-E-000-v0.md`
  - story：`../story/STORY-E-000-001-demo.md`

---

## 1. 目标与范围对齐（引用上游，不重写）

- 目标：提供一页静态原型 + 填好的文档，满足 Gate B（UI 证据）与 Gate C（Story+Slice）。
- Out of Scope：任何真实后端/鉴权/日志接入。
- 验收口径摘要：见 Story AC（空/成功/失败态可切换；文档链接存在）。
- 可观测性：示例不接入埋点，仅在文档注明。

---

## 2. 现状与约束（必须先读代码）

### 2.1 相关代码/系统现状

- 本仓库暂无业务代码，仅有 docs 与 agents 配置。
- 原型可放在 `docs/.../prototypes/index.html`，浏览器直接打开。

### 2.2 复用清单

- 直接复用模板：`docs/lib/templates/tpl-prototype-index.html`（样式参考，按需简化）。
- 复用文档模板：PRD/Story/Slice/Task/Proj。

### 2.3 硬约束

- 不依赖网络或后端；需纯静态可运行。
- 目录结构必须符合 `workflow-overview.md`。

---

## 3. 方案总览（1 页能讲清）

- 方案一句话：提供静态 HTML 原型演示状态切换，并链接到示例文档。
- 架构变化概览：无代码层架构；仅文档+静态资源。
- 关键 trade-off：选择纯静态，牺牲真实数据，换取零依赖可复制性。
- 影响面：新增 `docs/E-000-Agent-Workflow-Demo/**` 示例目录。

---

## 4. 详细设计

### 4.1 模块/服务边界与职责

- `prototypes/index.html`：前端状态机（empty/loading/success/error），展示主路径与链接。
- 文档子目录：PRD/Story/Slice/Tech/Task/Proj 示例。

### 4.2 数据模型与迁移策略

- 无持久化数据；状态枚举保存在前端脚本。

### 4.3 API / 事件 / 任务设计

- 无 API；前端按钮事件切换状态。

### 4.4 安全 / 权限 / 审计

- 公开静态文件，无鉴权。

### 4.5 可观测性（工程侧）

- 示例不接入日志/指标；真实项目可在复制后自行添加。

### 4.6 失败模式与可靠性

- 失败态以前端模拟；提供重试按钮恢复成功态。

---

## 5. 上线策略（MVP→演进）

- 以静态文件入库即上线；无灰度需求。
- 回滚策略：移除示例目录或恢复上一版。

---

## 6. 风险、未决项与升级点

- `[RISK]`：示例可能与未来基线不一致。
- `[OPEN]`：是否需要 biz-overview/ADR 示例一起提供。
- 需要升级给：
  - `prd`：示例字段或路径变动时同步。
  - `proj`：Gate 清单变更时更新 PROJ 示例。

---

## 7. 任务拆解建议（供 proj/dev 落 TASK）

- TASK-E-000-DOC-001：编写并验证示例文档 + 原型（依赖：无）。
- TASK-E-000-DOC-002（可选）：添加录屏/截图入口（依赖：原型完成）。

---

## 8. 与基线冲突（如有）

- 无基线冲突；保持与模板目录一致。
